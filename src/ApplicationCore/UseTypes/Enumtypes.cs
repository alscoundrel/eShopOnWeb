

namespace ApplicationCore.UseTypes
{
    public enum Ordination{
        ASC,
        DESC
    }

    public enum NamesOrderBy{
        Featured,
        Name,
        Price
    }

    public enum MediaOptions{
        EMail,
        SMS
    }

    public enum OrderStatus{
        Submitted,
        Accepted,
        Pending,
        Confirmed,
        Canceled,
        Sent,
        Delivered,
        Returned
    }
}